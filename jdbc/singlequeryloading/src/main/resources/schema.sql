CREATE TABLE PET_OWNER
(
    ID   SERIAL PRIMARY KEY,
    NAME VARCHAR(255)
);

CREATE TABLE CAT
(
    PET_OWNER     INT,
    PET_OWNER_KEY INT,
    NAME          VARCHAR(255)
);

CREATE TABLE DOG
(
    PET_OWNER     INT,
    PET_OWNER_KEY INT,
    NAME          VARCHAR(255)
);

CREATE TABLE FISH
(
    PET_OWNER     INT,
    PET_OWNER_KEY INT,
    NAME          VARCHAR(255)
);

