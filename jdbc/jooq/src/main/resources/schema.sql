CREATE TABLE IF NOT EXISTS category (
                                        id
                                        INTEGER
                                        AUTO_INCREMENT
                                        PRIMARY
                                        KEY,
                                        name
                                        VARCHAR
(
                                        100
),
  description VARCHAR(2000),
  age_group VARCHAR(20)
);
